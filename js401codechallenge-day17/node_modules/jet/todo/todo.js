var Jet = require("jet");
var $ = require("jquery");
var App = Jet.use("./index.html");

// config
var Todos = new App.Controller();

// model logic
Todos.Model.create({
    items: [],
    item: function() {
        Todos.keyTrigger.call()
    }
});

// view logic
Todos.View.create({
    main: "#main",
    item: "#item",
    list: "#list",
    footer: "#footer"
});

// controller logic
Todos.createEvent({
    keyTrigger: document.getElementById("input").onkeyup = function(event) {
      if(event.keyCode === 13) {
        Main.Model.push(document.getElementById("input").value);
      }
    }

    }
});

Todos.main.on({
    init: function() {
        Todos.item.on({
            "itemName": function() {} // do something with all these empty functions!!!
        });
        Todos.list.on({
            "items": function() {}
        });
        Todos.footer.on({
            "itemCount": function() {
                Todos.View.render(items.length);
            }
        });
    }
});